# 本周科研流水账（2026-06-25）

> 重要说明：这份总结原本应该读取 `radar-async-preprocessing` 仓库过去 5 天的 Git Diff、`run_log.txt` 的最终实验指标，以及 `results/` 目录里的数值和可视化结果。  
> 但本次自动任务实际运行时，当前环境没有挂载到这个目标仓库，也没有找到对应日志和结果文件。所以下面只写已经真实核验到的情况，不编造物理代码改动、实验指标或图像结论。

## 1. 本次实际读到了什么

- 当前工作区：`/workspace`
- 当前可见 Git 仓库远端：`LHZ-lny/Cursor`
- 当前本地分支：`cursor/bc-64e7c885-c805-4f53-be07-c8285318089e-f276`
- 当前工作区顶层内容：
  - `README.md`
  - `paper_daily.md`
  - `paper_daily_2026-06-12.md`
  - `ideas/`
  - `周报/`
  - `周计划/`
  - `工作素材/`
- 当前工作区一开始没有 `radar-async-preprocessing/` 目录。
- `/workspace`、`/home/ubuntu`、`/tmp` 下都没有找到：
  - `radar-async-preprocessing/` 目标仓库
  - `run_log.txt`
  - `results/` 实验结果目录

## 2. 过去 5 天 Git Diff 的读取情况

这次没有读到 `radar-async-preprocessing` 仓库本体，所以不能提取它过去 5 天的真实 Git Diff。

我能确认的是：

- `gh repo view LHZ-lny/radar-async-preprocessing` 无法解析到仓库。
- `gh repo list` 当前认证账号可见仓库里没有匹配 `radar`、`async`、`preprocess`、`processing` 的仓库。
- `gh search repos radar-async-preprocessing` 没有搜到同名公开仓库。
- 当前仓库历史里只发现过上一周生成的状态文件：`radar-async-preprocessing/实验结果总结_2026-06-18.md`。
- 当前仓库过去 5 天可见的变化主要是科研阅读文档、想法草稿、周报等，不是雷达异步预处理代码。

大白话说：这次还是没有摸到真正的实验代码仓库，只看到了外层文档仓库和上一次自动任务留下的“缺材料说明”。

## 3. run_log.txt 最终实验指标

本次没有找到 `run_log.txt`，因此没有办法确认最终实验指标。

目前缺失的信息包括：

- 最终 loss
- accuracy / precision / recall / F1 / AUC 等分类指标
- 验证集和测试集表现
- 最优 checkpoint 对应轮次
- early stopping 是否触发
- 是否出现 loss 震荡、梯度爆炸、过拟合或训练中断

所以不能写“这周指标提升了多少”，也不能判断某个改动到底是有效、无效，还是只是把流程跑通了。

## 4. results/ 下的数值和可视化结果

本次没有找到 `results/` 目录，因此没有读到任何真实结果文件或可视化图片。

目前无法总结：

- CSV / JSON / TXT 表格里的数值结果
- 训练曲线或验证曲线的收敛趋势
- 雷达预处理前后信号图的差异
- range-Doppler、点云、热力图或检测结果图里的变化
- 是否存在虚警、漏检、噪声残留、目标断裂、时间错位等可视化现象
- 消融实验中哪个模块贡献最大

换句话说，现在没有图、没有表、没有日志，不能靠猜来写“视觉效果更干净”或“目标检测更稳定”。

## 5. 本周科研流水账

这周这个自动任务想做的事情很明确：把 `radar-async-preprocessing` 里最近 5 天到底改了哪些物理代码、实验最后跑成什么样、结果图说明了什么，整理成一份看得懂的科研流水账。

但本次实际拿到的材料不够。当前环境里没有目标仓库，没有实验日志，也没有结果图。上一周的自动总结也记录过类似问题：当时同样没有访问到 `radar-async-preprocessing`，只能写缺失说明。

所以这周能负责任地写出来的流水账是：

1. 自动任务按要求开始检查 `radar-async-preprocessing`。
2. 在 `/workspace` 下没有找到目标仓库。
3. 继续搜索 `/home/ubuntu` 和 `/tmp`，也没有找到目标仓库、`run_log.txt` 或 `results/`。
4. 检查 GitHub 可访问仓库，没有找到可解析的 `LHZ-lny/radar-async-preprocessing`。
5. 检查当前仓库历史，只看到上一周生成过一份“目标实验仓库不可访问”的状态总结。
6. 因为没有真实物理代码 diff 和实验输出，本周不能形成关于算法、预处理链路或实验效果的科研结论。

用最直白的话说：本周自动任务没有拿到雷达实验的“原材料”。没有代码改动、没有日志指标、没有结果图，就不能把外层文档仓库里的内容硬说成雷达实验进展。

## 6. 这次不能下的结论

下面这些结论这次都不能写，因为没有真实依据：

- 不能说异步预处理逻辑被改进了。
- 不能说物理建模更合理了。
- 不能说时间戳对齐更准了。
- 不能说噪声被压下去了。
- 不能说模型准确率、F1 或 AUC 提升了。
- 不能说可视化结果更清晰了。
- 不能说某个模块的消融结果证明它有效。

## 7. 下次需要补齐什么

为了下次真的写出“本周科研流水账”，自动任务需要能访问到这些文件：

```text
radar-async-preprocessing/.git/
radar-async-preprocessing/run_log.txt
radar-async-preprocessing/results/
```

建议至少保证 `results/` 里包含以下一种或多种文件：

- 指标表：`*.csv`、`*.json`、`*.txt`、`*.md`
- 曲线图：loss、accuracy、F1、AUC 等
- 雷达中间结果图：预处理前后对比、range-Doppler 图、点云图、检测图
- 消融结果：不同预处理模块开关下的指标对比

拿到这些以后，总结就可以按下面方式写：

1. 这周代码到底改了哪几块。
2. 每块改动想解决什么实验问题。
3. 最终指标是变好、变坏，还是差不多。
4. 结果图里有没有能肉眼看出来的变化。
5. 哪些结论比较稳，哪些还需要继续补实验。

## 8. 一句话结论

本次没有访问到 `radar-async-preprocessing` 的真实 Git Diff、`run_log.txt` 和 `results/`，因此不能基于物理代码改动和实验结果写科研结论；这份文件记录的是 2026-06-25 自动检查的真实状态和缺失项。
