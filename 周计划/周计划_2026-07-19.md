本周 hypothesis：AAAI 主线不再继续扩散新机制，而把 7/14 的 ST-FDN 收缩成可跑通的最小闭环：在合成 irregular-event 数据上验证“solver-trace/roughness proxy 的 reference-bank front-door readout 能降低 sampling-policy shortcut”，先跑通训练、推理和指标落盘。
本周 killer experiment：实现一个单文件 smoke test（synthetic train/test policy shift + baseline event encoder + ST-FDN proxy trace + 3 个指标：in-policy accuracy、worst-policy accuracy、trace leakage），用一次命令跑完并生成 `results/stfdn_smoke_metrics.json`，不接真实雷达仓库、不做大规模消融。
预计产出：`experiments/stfdn_smoke.py`、`results/stfdn_smoke_metrics.json` 和一页结果说明；若 smoke test 跑通且 worst-policy/trace-leakage 方向正确，再把 ST-FDN 写入 AAAI 方法主线，否则回退到只修环境证据链。
